<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Consulta CEP Jarinu - Gestão de Endereços</title>
  <meta name="description" content="Sistema de consulta e cadastro de CEP para Jarinu/SP. Pesquise por logradouro, bairro ou CEP." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <div class="app">
    <!-- Sidebar -->
    <aside class="sidebar" id="sidebar">
      <div class="sidebar-header">
        <div class="sidebar-logo">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"/><circle cx="12" cy="10" r="3"/></svg>
        </div>
        <div class="sidebar-title">
          <h2>CEP Jarinu</h2>
          <p>Gestão de Endereços</p>
        </div>
      </div>
      <nav class="sidebar-nav">
        <span class="sidebar-label">Menu</span>
        <a href="#" class="nav-item active" data-page="consulta" onclick="showPage('consulta')">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
          <span>Consulta CEP</span>
        </a>
        <a href="#" class="nav-item" data-page="cadastro" onclick="showPage('cadastro')">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M8 12h8"/><path d="M12 8v8"/></svg>
          <span>Cadastro</span>
        </a>
      </nav>
    </aside>
    <!-- Main -->
    <div class="main">
      <header class="topbar">
        <button class="sidebar-toggle" onclick="toggleSidebar()">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="3" x2="21" y1="6" y2="6"/><line x1="3" x2="21" y1="12" y2="12"/><line x1="3" x2="21" y1="18" y2="18"/></svg>
        </button>
        <span class="topbar-text">Painel de Endereços — Jarinu/SP</span>
      </header>
      <main class="content">
        <!-- Consulta Page -->
        <section id="page-consulta" class="page active">
          <div class="page-header">
            <h1>Consulta CEP Jarinu</h1>
            <p>Pesquise por logradouro, bairro ou CEP</p>
          </div>
          <div class="stats-grid">
            <div class="stat-card fade-in" style="--delay: 0s">
              <div class="stat-icon icon-primary">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"/><circle cx="12" cy="10" r="3"/></svg>
              </div>
              <div>
                <p class="stat-value" id="stat-total">0</p>
                <p class="stat-label">Total de Endereços</p>
              </div>
            </div>
            <div class="stat-card fade-in" style="--delay: 0.1s">
              <div class="stat-icon icon-success">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="16" height="20" x="4" y="2" rx="2" ry="2"/><path d="M9 22v-4h6v4"/><path d="M8 6h.01"/><path d="M16 6h.01"/><path d="M12 6h.01"/><path d="M12 10h.01"/><path d="M12 14h.01"/><path d="M16 10h.01"/><path d="M16 14h.01"/><path d="M8 10h.01"/><path d="M8 14h.01"/></svg>
              </div>
              <div>
                <p class="stat-value" id="stat-ativos">0</p>
                <p class="stat-label">Ativos</p>
              </div>
            </div>
            <div class="stat-card fade-in" style="--delay: 0.2s">
              <div class="stat-icon icon-destructive">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="4" x2="20" y1="9" y2="9"/><line x1="4" x2="20" y1="15" y2="15"/><line x1="10" x2="8" y1="3" y2="21"/><line x1="16" x2="14" y1="3" y2="21"/></svg>
              </div>
              <div>
                <p class="stat-value" id="stat-inativos">0</p>
                <p class="stat-label">Inativos</p>
              </div>
            </div>
          </div>
          <div class="card">
            <div class="card-header">
              <h3 class="card-title">Buscar Endereço</h3>
            </div>
            <div class="card-body">
              <div class="search-input-wrap">
                <svg class="search-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
                <input type="text" id="searchInput" class="input search-input" placeholder="Digite o nome da rua, bairro ou CEP..." oninput="filterTable()" />
              </div>
            </div>
          </div>
          <div class="card fade-in" style="--delay: 0.2s">
            <div class="card-header">
              <h3 class="card-title">Resultados (<span id="result-count">0</span>)</h3>
            </div>
            <div class="card-body no-padding">
              <table class="table">
                <thead>
                  <tr>
                    <th>Logradouro</th>
                    <th>Bairro</th>
                    <th>CEP</th>
                    <th>Status</th>
                  </tr>
                </thead>
                <tbody id="tableBody"></tbody>
              </table>
            </div>
          </div>
        </section>
        <!-- Cadastro Page -->
        <section id="page-cadastro" class="page">
          <div class="page-header">
            <h1>Cadastro de Novos Endereços</h1>
            <p>Adicione novos endereços ao sistema</p>
          </div>
          <div class="card card-form fade-in">
            <div class="card-header">
              <h3 class="card-title">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="color: var(--primary)"><circle cx="12" cy="12" r="10"/><path d="M8 12h8"/><path d="M12 8v8"/></svg>
                Novo Endereço
              </h3>
              <p class="card-description">Preencha os dados abaixo. A exclusão é feita diretamente pelo banco de dados.</p>
            </div>
            <div class="card-body">
              <form id="cadastroForm" onsubmit="handleCadastro(event)">
                <div class="form-group">
                  <label for="logradouro">Logradouro</label>
                  <input type="text" id="logradouro" class="input" placeholder="Ex: Rua São Paulo" maxlength="150" />
                </div>
                <div class="form-row">
                  <div class="form-group">
                    <label for="bairro">Bairro</label>
                    <input type="text" id="bairro" class="input" placeholder="Ex: Centro" maxlength="100" />
                  </div>
                  <div class="form-group">
                    <label for="cep">CEP</label>
                    <input type="text" id="cep" class="input" placeholder="Ex: 13240-000" maxlength="10" />
                  </div>
                </div>
                <div class="form-group">
                  <label for="status">Status</label>
                  <select id="status" class="input select">
                    <option value="ativo">Ativo</option>
                    <option value="inativo">Inativo</option>
                  </select>
                </div>
                <button type="submit" class="btn-primary">Cadastrar Endereço</button>
              </form>
            </div>
          </div>
        </section>
      </main>
    </div>
  </div>
  <!-- Toast -->
  <div id="toast" class="toast hidden">
    <div class="toast-title" id="toast-title"></div>
    <div class="toast-desc" id="toast-desc"></div>
  </div>
  <script src="script.js"></script>
</body>
</html>





css
/* ── Variables ── */
:root {
  --background: 200 20% 97%;
  --foreground: 200 30% 12%;
  --card: 0 0% 100%;
  --card-foreground: 200 30% 12%;
  --primary: 174 60% 40%;
  --primary-foreground: 0 0% 100%;
  --secondary: 200 15% 93%;
  --secondary-foreground: 200 30% 20%;
  --muted: 200 15% 93%;
  --muted-foreground: 200 10% 50%;
  --accent: 38 90% 55%;
  --accent-foreground: 0 0% 100%;
  --destructive: 0 72% 55%;
  --destructive-foreground: 0 0% 100%;
  --border: 200 15% 88%;
  --input: 200 15% 88%;
  --ring: 174 60% 40%;
  --radius: 0.75rem;
  --sidebar-bg: 200 25% 14%;
  --sidebar-fg: 200 10% 85%;
  --sidebar-primary: 174 60% 50%;
  --sidebar-accent: 200 20% 20%;
  --sidebar-accent-fg: 200 10% 90%;
  --sidebar-border: 200 20% 22%;
  --success: 152 60% 42%;
  --success-foreground: 0 0% 100%;
  --warning: 38 90% 55%;
  --info: 210 70% 55%;
}
/* ── Reset ── */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
body {
  font-family: 'Inter', sans-serif;
  background: hsl(var(--background));
  color: hsl(var(--foreground));
  -webkit-font-smoothing: antialiased;
}
/* ── Layout ── */
.app { display: flex; min-height: 100vh; }
/* ── Sidebar ── */
.sidebar {
  width: 250px;
  background: linear-gradient(180deg, hsl(200 25% 14%), hsl(200 30% 10%));
  display: flex;
  flex-direction: column;
  flex-shrink: 0;
  transition: width 0.3s ease;
}
.sidebar.collapsed { width: 60px; }
.sidebar.collapsed .sidebar-title,
.sidebar.collapsed .nav-item span,
.sidebar.collapsed .sidebar-label { display: none; }
.sidebar-header {
  padding: 16px;
  display: flex;
  align-items: center;
  gap: 12px;
}
.sidebar-logo {
  width: 36px; height: 36px;
  border-radius: 8px;
  background: linear-gradient(135deg, hsl(174 60% 40%), hsl(174 60% 30%));
  display: flex; align-items: center; justify-content: center;
  color: hsl(var(--primary-foreground));
  flex-shrink: 0;
}
.sidebar-title h2 {
  font-size: 14px; font-weight: 700;
  color: hsl(var(--sidebar-primary));
}
.sidebar-title p {
  font-size: 12px;
  color: hsl(var(--sidebar-fg) / 0.6);
}
.sidebar-nav { padding: 0 8px; }
.sidebar-label {
  font-size: 11px; text-transform: uppercase; letter-spacing: 0.05em;
  color: hsl(var(--sidebar-fg) / 0.4);
  padding: 8px 12px 4px;
}
.nav-item {
  display: flex; align-items: center; gap: 8px;
  padding: 8px 12px; border-radius: var(--radius);
  color: hsl(var(--sidebar-fg) / 0.7);
  text-decoration: none; font-size: 14px;
  transition: all 0.15s ease;
  margin-bottom: 2px;
}
.nav-item:hover {
  background: hsl(var(--sidebar-accent));
  color: hsl(var(--sidebar-accent-fg));
}
.nav-item.active {
  background: hsl(var(--sidebar-accent));
  color: hsl(var(--sidebar-primary));
  font-weight: 600;
}
/* ── Main ── */
.main { flex: 1; display: flex; flex-direction: column; min-width: 0; }
.topbar {
  height: 56px; display: flex; align-items: center;
  padding: 0 16px; gap: 16px;
  border-bottom: 1px solid hsl(var(--border));
  background: hsl(var(--card) / 0.6);
  backdrop-filter: blur(8px);
  position: sticky; top: 0; z-index: 10;
}
.topbar-text {
  font-size: 14px; font-weight: 600;
  color: hsl(var(--muted-foreground));
}
.sidebar-toggle {
  background: none; border: none; cursor: pointer;
  color: hsl(var(--muted-foreground));
  padding: 4px; border-radius: 6px;
}
.sidebar-toggle:hover { background: hsl(var(--secondary)); }
.content { flex: 1; padding: 24px; }
/* ── Pages ── */
.page { display: none; }
.page.active { display: block; }
.page-header { margin-bottom: 24px; }
.page-header h1 { font-size: 24px; font-weight: 700; color: hsl(var(--foreground)); }
.page-header p { font-size: 14px; color: hsl(var(--muted-foreground)); margin-top: 4px; }
/* ── Stats ── */
.stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin-bottom: 24px; }
.stat-card {
  background: hsl(var(--card) / 0.8);
  backdrop-filter: blur(4px);
  border: 1px solid hsl(var(--border));
  border-radius: var(--radius);
  padding: 16px;
  display: flex; align-items: center; gap: 16px;
  box-shadow: 0 1px 3px hsl(var(--foreground) / 0.04);
}
.stat-icon {
  width: 40px; height: 40px; border-radius: 8px;
  background: hsl(var(--secondary));
  display: flex; align-items: center; justify-content: center;
}
.icon-primary { color: hsl(var(--primary)); }
.icon-success { color: hsl(var(--success)); }
.icon-destructive { color: hsl(var(--destructive)); }
.stat-value { font-size: 24px; font-weight: 700; color: hsl(var(--foreground)); }
.stat-label { font-size: 12px; color: hsl(var(--muted-foreground)); }
/* ── Card ── */
.card {
  background: hsl(var(--card) / 0.8);
  backdrop-filter: blur(4px);
  border: 1px solid hsl(var(--border));
  border-radius: var(--radius);
  box-shadow: 0 1px 3px hsl(var(--foreground) / 0.04);
  margin-bottom: 24px;
}
.card-header { padding: 20px 20px 12px; }
.card-title {
  font-size: 15px; font-weight: 600;
  color: hsl(var(--foreground));
  display: flex; align-items: center; gap: 8px;
}
.card-description { font-size: 13px; color: hsl(var(--muted-foreground)); margin-top: 4px; }
.card-body { padding: 0 20px 20px; }
.card-body.no-padding { padding: 0; }
/* ── Input ── */
.input {
  width: 100%; height: 40px; padding: 0 12px;
  border: 1px solid hsl(var(--input));
  border-radius: calc(var(--radius) - 2px);
  background: hsl(var(--background));
  color: hsl(var(--foreground));
  font-size: 14px; font-family: inherit;
  transition: border-color 0.15s, box-shadow 0.15s;
  outline: none;
}
.input:focus {
  border-color: hsl(var(--ring));
  box-shadow: 0 0 0 2px hsl(var(--ring) / 0.2);
}
.input::placeholder { color: hsl(var(--muted-foreground)); }
.select { cursor: pointer; appearance: auto; }
/* ── Search ── */
.search-input-wrap { position: relative; }
.search-icon {
  position: absolute; left: 12px; top: 50%; transform: translateY(-50%);
  color: hsl(var(--muted-foreground)); pointer-events: none;
}
.search-input { padding-left: 36px; }
/* ── Table ── */
.table { width: 100%; border-collapse: collapse; font-size: 14px; }
.table thead tr { background: hsl(var(--secondary) / 0.5); }
.table th {
  text-align: left; padding: 12px 16px;
  font-weight: 600; font-size: 13px;
  color: hsl(var(--foreground));
}
.table td { padding: 12px 16px; border-top: 1px solid hsl(var(--border)); }
.table tbody tr { transition: background 0.15s; }
.table tbody tr:hover { background: hsl(var(--secondary) / 0.3); }
.table .font-mono { font-family: 'JetBrains Mono', monospace; font-size: 13px; }
.table .font-medium { font-weight: 500; }
.table .empty-row td { text-align: center; padding: 40px 16px; color: hsl(var(--muted-foreground)); }
/* ── Badge ── */
.badge {
  display: inline-flex; align-items: center; gap: 6px;
  padding: 2px 10px; border-radius: 999px;
  font-size: 12px; font-weight: 500;
  border: 1px solid;
}
.badge-ativo {
  background: hsl(var(--success) / 0.15);
  color: hsl(var(--success));
  border-color: hsl(var(--success) / 0.3);
}
.badge-inativo {
  background: hsl(var(--destructive) / 0.15);
  color: hsl(var(--destructive));
  border-color: hsl(var(--destructive) / 0.3);
}
.badge-dot {
  width: 6px; height: 6px; border-radius: 50%;
}
.badge-ativo .badge-dot { background: hsl(var(--success)); }
.badge-inativo .badge-dot { background: hsl(var(--destructive)); }
/* ── Form ── */
.card-form { max-width: 640px; }
.form-group { margin-bottom: 20px; }
.form-group label {
  display: block; font-size: 14px; font-weight: 500;
  color: hsl(var(--foreground)); margin-bottom: 6px;
}
.form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
.btn-primary {
  width: 100%; height: 42px; border: none; border-radius: calc(var(--radius) - 2px);
  background: linear-gradient(135deg, hsl(174 60% 40%), hsl(174 60% 30%));
  color: hsl(var(--primary-foreground));
  font-size: 14px; font-weight: 600; font-family: inherit;
  cursor: pointer; transition: opacity 0.15s;
}
.btn-primary:hover { opacity: 0.9; }
/* ── Toast ── */
.toast {
  position: fixed; bottom: 24px; right: 24px;
  background: hsl(var(--card));
  border: 1px solid hsl(var(--border));
  border-radius: var(--radius);
  padding: 16px 20px; min-width: 280px;
  box-shadow: 0 8px 30px hsl(var(--foreground) / 0.1);
  z-index: 100;
  animation: slideUp 0.3s ease;
}
.toast.hidden { display: none; }
.toast.toast-error { border-left: 4px solid hsl(var(--destructive)); }
.toast.toast-success { border-left: 4px solid hsl(var(--success)); }
.toast-title { font-size: 14px; font-weight: 600; color: hsl(var(--foreground)); }
.toast-desc { font-size: 13px; color: hsl(var(--muted-foreground)); margin-top: 2px; }
/* ── Animations ── */
@keyframes slideUp {
  from { opacity: 0; transform: translateY(12px); }
  to { opacity: 1; transform: translateY(0); }
}
.fade-in {
  animation: fadeIn 0.4s ease both;
  animation-delay: var(--delay, 0s);
}
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(8px); }
  to { opacity: 1; transform: translateY(0); }
}
/* ── Responsive ── */
@media (max-width: 768px) {
  .sidebar { position: fixed; z-index: 50; height: 100vh; transform: translateX(-100%); }
  .sidebar.open { transform: translateX(0); }
  .stats-grid { grid-template-columns: 1fr; }
  .form-row { grid-template-columns: 1fr; }
}



script.js
// ── Data ──
let addresses = [
  { id: "1", logradouro: "Rua São Paulo", bairro: "Centro", cep: "13240-000", status: "ativo" },
  { id: "2", logradouro: "Avenida Brasil", bairro: "Jardim Primavera", cep: "13240-010", status: "ativo" },
  { id: "3", logradouro: "Rua das Flores", bairro: "Vila Nova", cep: "13240-020", status: "inativo" },
  { id: "4", logradouro: "Rua Quinze de Novembro", bairro: "Centro", cep: "13240-030", status: "ativo" },
  { id: "5", logradouro: "Rua Tiradentes", bairro: "Jardim Europa", cep: "13240-040", status: "ativo" },
  { id: "6", logradouro: "Alameda dos Ipês", bairro: "Parque das Árvores", cep: "13240-050", status: "inativo" },
  { id: "7", logradouro: "Rua Dom Pedro II", bairro: "Centro", cep: "13240-060", status: "ativo" },
  { id: "8", logradouro: "Rua Marechal Deodoro", bairro: "Vila Industrial", cep: "13240-070", status: "ativo" },
];
// ── Init ──
document.addEventListener("DOMContentLoaded", () => {
  updateStats();
  renderTable(addresses);
});
// ── Navigation ──
function showPage(page) {
  document.querySelectorAll(".page").forEach((p) => p.classList.remove("active"));
  document.getElementById("page-" + page).classList.add("active");
  document.querySelectorAll(".nav-item").forEach((n) => n.classList.remove("active"));
  document.querySelector(`.nav-item[data-page="${page}"]`).classList.add("active");
  // Close sidebar on mobile
  if (window.innerWidth <= 768) {
    document.getElementById("sidebar").classList.remove("open");
  }
}
function toggleSidebar() {
  const sidebar = document.getElementById("sidebar");
  if (window.innerWidth <= 768) {
    sidebar.classList.toggle("open");
  } else {
    sidebar.classList.toggle("collapsed");
  }
}
// ── Stats ──
function updateStats() {
  document.getElementById("stat-total").textContent = addresses.length;
  document.getElementById("stat-ativos").textContent = addresses.filter((a) => a.status === "ativo").length;
  document.getElementById("stat-inativos").textContent = addresses.filter((a) => a.status === "inativo").length;
}
// ── Table ──
function renderTable(data) {
  const tbody = document.getElementById("tableBody");
  document.getElementById("result-count").textContent = data.length;
  if (data.length === 0) {
    tbody.innerHTML = `<tr class="empty-row"><td colspan="4">Nenhum endereço encontrado.</td></tr>`;
    return;
  }
  tbody.innerHTML = data
    .map(
      (addr) => `
    <tr>
      <td class="font-medium">${addr.logradouro}</td>
      <td>${addr.bairro}</td>
      <td class="font-mono">${addr.cep}</td>
      <td>
        <span class="badge badge-${addr.status}">
          <span class="badge-dot"></span>
          ${addr.status === "ativo" ? "Ativo" : "Inativo"}
        </span>
      </td>
    </tr>
  `
    )
    .join("");
}
// ── Filter ──
function filterTable() {
  const q = document.getElementById("searchInput").value.toLowerCase().trim();
  if (!q) {
    renderTable(addresses);
    return;
  }
  const filtered = addresses.filter(
    (a) =>
      a.logradouro.toLowerCase().includes(q) ||
      a.bairro.toLowerCase().includes(q) ||
      a.cep.includes(q)
  );
  renderTable(filtered);
}
// ── Cadastro ──
function handleCadastro(e) {
  e.preventDefault();
  const logradouro = document.getElementById("logradouro").value.trim();
  const bairro = document.getElementById("bairro").value.trim();
  const cep = document.getElementById("cep").value.trim();
  const status = document.getElementById("status").value;
  if (!logradouro || !bairro || !cep) {
    showToast("Campos obrigatórios", "Preencha todos os campos antes de cadastrar.", "error");
    return;
  }
  addresses.push({
    id: Date.now().toString(),
    logradouro,
    bairro,
    cep,
    status,
  });
  updateStats();
  renderTable(addresses);
  // Reset form
  document.getElementById("cadastroForm").reset();
  showToast("Endereço cadastrado!", `${logradouro} foi adicionado com sucesso.`, "success");
}
// ── Toast ──
let toastTimeout;
function showToast(title, desc, type) {
  const toast = document.getElementById("toast");
  document.getElementById("toast-title").textContent = title;
  document.getElementById("toast-desc").textContent = desc;
  toast.className = "toast toast-" + type;
  clearTimeout(toastTimeout);
  toastTimeout = setTimeout(() => {
    toast.classList.add("hidden");
  }, 3500);
}